{
  "name": "rookie-staff-zenesys",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend run build",
    "start": "node backend/server.js",
    "serve": "npm run build && npm run start"
  }
}
